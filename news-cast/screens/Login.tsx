import { StatusBar } from "expo-status-bar";
import React from "react";
import { StyleSheet, View } from "react-native";
import {
  NativeBaseProvider,
  Button,
  useToken,
  useTheme,
  Box,
  Text,
} from "native-base";

export default function Login() {
  return <Box>ugiukguikguigug</Box>;
}
